// Crediti: Vitale Carlo Simone

var displayCalculation = {
    add: function(calculation, bits) {
        var mainDiv = document.getElementById("operationResult");

        // Remove ALL html easy n quicl
        mainDiv.innerHTML = "";

        for (var i = 0; i < calculation.proc.length; i++) {
            
        }
    }
}

// Crediti: Vitale Carlo Simone